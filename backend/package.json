{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "migrate": "node scripts/migrateDatabase.js",
    "optimize-db": "node scripts/optimizeDatabase.js",
    "optimize-db-with-samples": "node scripts/optimizeDatabase.js --sample-data",
    "generate-products": "node scripts/generateSampleProducts.js",
    "generate-products-clean": "node scripts/generateSampleProducts.js --clean",
    "generate-products-test": "node scripts/generateSampleProducts.js --clean --test-products",
    "generate-users": "node scripts/generateSampleUsers.js",
    "generate-users-clean": "node scripts/generateSampleUsers.js --clean",
    "generate-users-random": "node scripts/generateSampleUsers.js --clean --random 100",
    "generate-users-test": "node scripts/generateSampleUsers.js --clean --test-users",
    "generate-orders": "node scripts/generateSampleOrders.js",
    "generate-orders-clean": "node scripts/generateSampleOrders.js --clean",
    "generate-orders-test": "node scripts/generateSampleOrders.js --clean --test-orders",
    "generate-all": "npm run generate-users-clean && npm run generate-products-clean && npm run generate-orders-clean",
    "setup": "npm run migrate && npm run optimize-db-with-samples && npm run generate-all",
    "setup-production": "node scripts/setup-production.js",
    "fix-products": "node scripts/fix-product-ids.js",
    "postinstall": "if [ \"$NODE_ENV\" == \"production\" ]; then npm run setup-production; fi",
    "start": "npm run setup-production && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-session": "^1.18.2",
    "helmet": "^7.1.0",
    "joi": "^17.11.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.17.0",
    "mongoose": "^8.1.3",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.7",
    "nodemon": "^3.0.3",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "passport-jwt": "^4.0.1",
    "passport-local": "^1.0.0",
    "stripe": "^14.7.0",
    "uuid": "^9.0.1",
    "winston": "^3.11.0"
  }
}
